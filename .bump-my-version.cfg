[bump-my-version]
current_version = "1.4.0"
commit = true
tag = true

[bump-my-version:file_patterns]
pyproject.toml =
    'version = "{current_version}"'
README.md =
    "Download the latest release (v{current_version})"
clone-vm.spec =
    "'vers': '{current_version}.0'"
create-master-vm.spec =
    "'vers': '{current_version}.0'"
web-app.spec =
    "'vers': '{current_version}.0'"
