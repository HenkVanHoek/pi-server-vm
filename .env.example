# .env.example
# This file is an example. Create a file named .env in the root of the project and add the following line.
# It contains the path to the web server's document root for MkDocs deployment.
# This can be a local path or a UNC path to a network share (e.g., \webserver\share\docs).

DOCS_DEPLOY_PATH=\\path\to\your\webserver\docs
